# PROGRESS

> Single source of truth for "where we are." Updated at the end of every phase per [feedback-phase-wrap]. If you're a fresh chat picking this up, read the **Bootstrap** section at the bottom first.

**Current position:** apps/api **MVP COMPLETE (17/17 phases)**. Phase 17 (README + docs polish) done: root README rewritten as the hiring-team-facing headline (live demo + seeded public-share links, what-it-demonstrates, decisions index → DECISIONS, stack, quality bar, CI badge), apps/api README expanded with the endpoint map, and the missing `LICENSE` (MIT) added. *Shawn-gated leftovers:* confirm the Fly deploy went green, add a `/docs` screenshot + Loom walkthrough to the README, and run the prod smoke (DEPLOY.md). **apps/web Phases 1–8 done** (Next.js 16 + npm + Tailwind v4, dark default; **BFF auth** — JWE session in an httpOnly same-origin cookie, tokens never reach the browser; app shell; landing + Try Demo; BFF data proxy + SSE client; **prompts CRUD + versions w/ Monaco**; **eval suites/cases + live SSE batch results**; real dashboard). Phase 8 also added `GET /api/v1/runs` to apps/api (api now 223 tests). **apps/web Phase 9 (ragent chat) was blocked on apps/ragent existing — so we built apps/ragent, now FEATURE-COMPLETE (14/14).** Phase 1 (bootstrap): `promptforge_ragent` FastAPI + `/health`, shared-`PF_`-env config + structlog/raw-ASGI middleware, Dockerfile, `fly.toml`, CI skeleton. **Phase 2 (DB models):** 5 models (Corpus/Document/Chunk/Conversation/Message); Chunk has dual nullable pgvector cols (1536/384) + partial ivfflat indexes. Schema-ownership locked: ragent owns models, apps/api owns the one migration history — migration `0009` hand-authored (vector DDL raw SQL, no pgvector dep in api) + `include_object` autogenerate guard; test/compose image moved to `pgvector/pgvector:pg17`. **Phase 3 (ingest pipeline):** parse (md/pdf/html/text) → chunk (token-window + overlap, tiktoken) → embed (per-corpus routing; OpenAI now, bge=Phase-12 seam) → persist, via `services/` + an idempotent `ingest_document` orchestrator. **Phase 4 (ingest worker):** queue-driven worker drives `ingest_document` off the shared `jobs` table (`kind="ingest_document"`; coexists with api's eval worker), trimmed `core/queue.py` (SKIP LOCKED, no ORM Job model), transient (RetriableEmbeddingError→requeue-with-backoff) vs terminal (FAILED) classification, `documents.raw_content` BYTEA (migration `0010`) for the detached worker, `ingest-worker` Fly process. Found+fixed: DB-clock requeue backoff (Docker/WSL2 clock-drift) + consume-loop survives re-raised transients. **Phase 5 (hybrid retrieval):** `hybrid_search` = pgvector cosine (per-corpus column) + BM25 in-process (rank-bm25) → RRF fusion (k=60), returns ranked `RetrievedChunk`s; integration test proves a lexical-only match surfaces via BM25 where dense alone wouldn't. **Phase 6 (optional rerank):** `rerank` cross-encoder (bge-reranker-base) reorders chunks when `PF_RERANK_ENABLED=true`, else passthrough; off by default, heavy dep behind the optional `rerank` extra (CI/image never install it; CI switched to `--extra dev`). **Phase 7 (agent tools):** `agent/tools.py` — 3 ReAct tools (`search_docs`/`fetch_passage`/`cite_sources`) as litellm function schemas + corpus/org-scoped handlers + `execute_tool` dispatcher; handlers return error dicts (not raises) so the loop can self-correct. **Phase 8 (agent loop):** `agent/loop.py` `run_agent` ReAct loop (litellm function-calling → execute_tool → loop), max-iter cap + circuit breaker on repeated tool+args → tool-less forced final answer; yields `tool_call`/`tool_result`/`answer` events for Phase-9 SSE. `services/system_prompt.py` fetches the system prompt live from apps/api (service JWT via shared secret) with TTL cache + default fallback. **Phase 9 (chat SSE):** `POST /api/v1/chat` — ragent's first HTTP surface: shared-HS256 auth, corpus resolution, load/create conversation, persist user msg, `run_agent` streamed over SSE (`conversation`/`tool_call`/`tool_result`/`answer`/`done`), persist assistant msg with citations + tool trail; BYOK via `X-Provider-Key`; `sse-starlette` dep. **Phase 10 (corpora API + upload):** `GET/POST /corpora` + `POST/GET /corpora/{id}/documents` — upload → `Document(raw_content)` + `enqueue_ingest` (async worker ingest), writes writer-gated (demo can't upload), 5 MB/file + 50 MB/corpus caps; `python-multipart` dep. **Phase 11 (seed):** apps/api seed creates the agent's system prompt; ragent resolves it from the shared DB by natural key (`services/platform.py`) + fetches the body over HTTP (closes the Phase-8 seam, drops the env ids); `seed.py` seeds 3 demo corpora into Demo Corp via real `ingest_document` (idempotent). **Phase 12 (embedding routing):** implemented the local bge-small (384-d) backend (Phase-3 seam) behind an opt-in `local-embeddings` extra (off by default like rerank); `embed_texts` gained `is_query` for bge's query instruction. **Phase 13 (deploy + smoke):** demo cost control — free hosted turns then BYOK, capped per-IP + **globally** (the VPN/IP-rotation backstop); `DemoUsage`/migration 0011 + `demo_quota.py` + chat 402-gate; ragent CD (full-suite `tests` + `deploy` jobs) + `DEPLOY-RAGENT.md` runbook (deploy/smoke are Shawn's). **Phase 14 (README + polish):** ragent README rewritten as the hiring-team headline + root README folds ragent in (badge, demonstrates bullet, feature-complete status). **apps/ragent feature-complete: 14/14, 93 tests; apps/api 223 passed.** Next: apps/web Phase 9 (ragent chat UI) now unblocked, + the Shawn-gated deploys/smokes. See the apps/ragent section.

**Previously (Phase 16, code-complete):** deploy + observability — **prod deploy + smoke is Shawn's to run** (like git). structlog wired (`core/logging.py`): JSON logs in prod, console when `PF_LOG_LEVEL=DEBUG`, request id bound via contextvars + echoed on `X-Request-ID`. The request-context middleware is **raw ASGI on purpose** so it doesn't buffer the SSE stream (BaseHTTPMiddleware would). OTel deliberately deferred — single gated enable-point documented in the `main.py` lifespan, no deps added (Shawn chose structlog-only + seam). `fly.toml` release now runs `sh -c 'alembic upgrade head && python -m promptforge_api.seed'` so every deploy migrates + (idempotently) seeds. DEPLOY.md rewritten as the real prod runbook (Neon not Fly-PG, hosted-key requirement for demo free runs, FLY_API_TOKEN for CD, full smoke incl. demo/shares/SSE/request-id). **221 tests pass** locally. Next: Phase 17 (README + DECISIONS polish), then apps/ragent + apps/web.

**Fresh-chat bootstrap:** if you're picking this up cold, read this file's Bootstrap section at the bottom first. Short version: **apps/api is MVP-complete (17/17)**; api + Postgres live on Fly+Neon; next is apps/web (Next.js) then apps/ragent. DECISIONS.md has 35+ defendable "Why" entries — read before any architecture conversation.

**Last verified locally:** 2026-06-04 — full suite **223 passed** (+1 for `GET /runs`), coverage 84.98%, no flake. ruff + ruff format + mypy --strict clean. Full suite needs Docker for testcontainers Postgres. (Note: `.venv` recreated this session pulled newer deps — ruff 0.5→0.15 reformatted 4 untouched test files; consider tightening pins.)

**Prod verified:** 2026-06-03 — DEPLOY.md smoke green against `https://promptforge-api.fly.dev` (8/8): health, openapi, docs, X-Request-ID header (structlog live), demo login (org=demo-corp, free_runs=5 → seed ran in release), read-only 403, both public share links resolve (eval report pass_rate=0.667). Demo mode + shares + seed-on-release + structured logging all confirmed live. **Live SSE batch stream also verified in prod 2026-06-03** (worker scaled to 1, fresh signup → suite → case → batch → `curl -N` the stream → `open` → `result` → `done`): queue → worker → LLM → judge → NOTIFY → SSE end-to-end on Fly. Worker scaled back to 0 after. **apps/api fully prod-verified.** **apps/web Phase 9 (ragent chat) verified end-to-end 2026-06-09 against live api + ragent** (demo → corpora → SSE chat → cited answer). apps/ragent prod confirmed live (cold-start ~45s).

---

## apps/api — 17 / 17 phases done ✅ (MVP complete; prod deploy + screenshots Shawn-gated)

- [x] **Phase 1 — Bootstrap.** repo init, pyproject (uv + hatchling), FastAPI hello, `/health`, Dockerfile, fly.toml, CI skeleton.
- [x] **Phase 2 — DB + Alembic.** Async SQLAlchemy 2.x, naming convention, async-mode Alembic via asyncpg only (no separate sync driver). Models: User, Org, Membership, OrgRole. Migration `20260523_0001`.
- [x] **Phase 3 — Auth + refresh tokens.** argon2id passwords, HS256 JWT, HMAC-stored refresh tokens with rotation + chain-revocation replay defense, API keys (`pf_live_<prefix>_<secret>` with argon2 hash + prefix lookup). Migration `20260525_0002`.
- [x] **Phase 4 — TenantRepository + tenancy contract.** Generic `TenantRepository[T]`, `get_repo(Model)` dep factory, `tests/tenancy/_helpers.py`, ApiKey tenancy contract applied.
- [x] **Phase 5 — Prompts + versions CRUD.** Prompt + PromptVersion models (append-only versions), visibility enum (private/org/public), routes for list/create/get/patch/delete + version create/list/get-by-id, pagination + q/tag/visibility filters. Migration `20260525_0003`. `TenantRepository.list/count` gained a `where` kwarg.
- [x] **Phase 6 — `core/prompts.py` typed templates.** `PromptVariable`, `PromptTemplate`, applicative `render()` (collects every error before raising), `fingerprint()`, wired into create routes so invalid templates return 422.
- [x] **Phase 7 — `core/async_utils.py`.** `retry` decorator (exponential/linear/constant + equal jitter), `TokenBucket` with injectable clock/sleep for testability, `rate_limited` decorator, `gather_bounded` w/ overloaded raise/collect.
- [x] **Phase 8 — `core/queue.py` + worker.** Postgres `SELECT FOR UPDATE SKIP LOCKED` queue w/ CTE-based atomic claim, `LISTEN/NOTIFY` fanout (channel `jobs` for enqueue events, `batch_<uuid>` for SSE eval progress), Job model on BIGSERIAL w/ partial index on queued rows, `ClaimedJob` async-context-manager (ack on clean exit / requeue-with-backoff on raise / mark failed after max_attempts), `workers/eval_worker.py` skeleton process w/ graceful SIGINT/SIGTERM. Migration `20260529_0004`. 8 integration tests cover happy path, kind filter, run_after, ack, requeue, terminal fail, SKIP LOCKED no-double-claim concurrency, batch_progress aggregation.
- [x] **Phase 9 — `services/llm.py`.** litellm async wrapper (`call_llm`). Transient-only retry (APIConnectionError, Timeout, RateLimitError, APIError) — auth/bad-request propagate immediately. Global TokenBucket (10/s) for the hosted demo key; BYOK calls skip the limiter (user hits their own quota). Typed `LLMResponse` dataclass w/ text/tokens/cost/latency/raw. Cost via `litellm.completion_cost`; falls back to None if pricing unknown. 9 unit tests w/ fully mocked `litellm.acompletion`.
- [x] **Phase 10 — Runs.** `Run` model (org_id-scoped, version_id FK, output/tokens/cost/latency/error/provider_response, `Numeric(12, 6)` for cost precision), migration `0005`. `POST /api/v1/versions/{id}/run` → resolves version via parent prompt (tenancy), validates inputs via `PromptTemplate.render`, calls `services.llm.call_llm`, persists Run row (including on LLM failure — error column captures it). `GET /api/v1/runs/{id}` via TenantRepository. Demo users must supply `X-Provider-Key` (BYOK). 7 e2e tests w/ mocked LLM (happy path, invalid vars 422, LLM-failure-persists, cross-org POST 404, cross-org GET 404, auth required, unknown version 404). 3 integration tests on the Run model. **Also fixed phase-8 NOTIFY bug** — Postgres `NOTIFY` doesn't accept bound params; switched to `pg_notify()` function form.

## apps/api — pending

- [x] **Phase 10.5 — DEPLOY SMOKE.** *Done 2026-06-01.* Decisions locked: Neon free tier over Fly MPG ($38/mo floor) / unmanaged Fly Postgres (no support) / Supabase (PgBouncer footgun). Postgres 17 across testcontainers + compose + prod. `Settings.async_database_url()` normalizer handles bare `postgresql://`, `postgres://`, and `sslmode` → `ssl` rewriting so any provider DSN works (unit-tested w/ 5 DSN shapes). Dockerfile: added `README.md` to early COPY so hatchling validates during `uv sync`. Two Fly machines provisioned (`api` + `worker`); worker can scale to 0 via `fly scale count worker=0` until Phase 11 — effective demo cost under $0.50/mo. **Smoke validated end-to-end against `https://promptforge-api.fly.dev`:** /health, /openapi.json, /docs, signup, /me, prompt create, run (the run without BYOK proved the failed-run-persists-with-error design works in prod). *TODO: screenshot /docs for README header.*
- [x] **Phase 11 — Eval engine.** EvalSuite/EvalCase/EvalBatch/EvalResult models w/ unique(batch, version, case) for idempotent re-runs, migration `0006`. Four judges (`services/judge.py`): exact (whitespace-trimmed), contains (case_sensitive flag), regex (re.IGNORECASE/MULTILINE/DOTALL flags), llm_judge (separate LLM call w/ rubric, JSON-only response, threshold default 0.7, clamped to [0,1]). `services/eval_runner.py` orchestrates per-job execution: render template → call_llm (failed-run pattern preserved) → grade → upsert EvalResult via `ON CONFLICT` → atomic completed_jobs increment → pg_notify on batch channel. Routes (`api/v1/evals.py`): POST suite, POST cases, POST run (enqueues version × case jobs onto Postgres queue), GET batch detail, **GET /eval-batches/{id}/stream** SSE endpoint using asyncpg `add_listener` on the batch channel with heartbeat + done event. Worker handler wired (`kind=eval_case` → `run_eval_case`). 14 unit tests for judges (incl. LLM-judge mocked w/ threshold + clamp + non-JSON + call-failure paths), 4 e2e batch tests w/ in-process worker drain. Added `sse-starlette` dep. Also fixed test conftest leaking the module-global asyncpg engine across tests.
- [x] **Phase 12 — Eval e2e.** *Done 2026-06-01.* Extracted the SSE event loop from `stream_batch` into a module-level, transport-agnostic `eval_event_stream(engine, batch_id)` generator (testable seam — see DECISIONS "Why test the SSE generator directly"). Added a terminal short-circuit: on subscribe, if the batch is already `done`/`failed`, emit `open`+`done` and close (fixes a fast-batch race where a client connecting after completion would sit on a silent channel until the 30s heartbeat). **5 new e2e tests:** live forward path (interleaved drain so `result` events stream before the terminal flip), already-done short-circuit, full HTTP-stack SSE through the real route + EventSourceResponse, cross-org stream → 404, **and a real-worker test that runs `_consume_forever` against the queue → handler → committed NOTIFY → SSE subscriber** (closes the [feedback-test-directness] gap: the other tests drive the handler in-process via `_run_one`, which doesn't exercise the worker's claim/dispatch loop). **Fixed a latent notify bug:** `eval_runner._notify` ran `pg_notify` after `session.commit()` in an uncommitted transaction → SSE events never delivered; moved NOTIFY inside the committed transaction. **Removed dead `queue.notify_batch`** — unused, and it used a separate `engine.begin()` transaction (the non-atomic anti-pattern we just moved away from). 198 tests pass. *TODOs (Shawn-gated): (1) screenshot Swagger `/docs` for README header — needs server run + capture (carries from Phase 10.5; dashboard screenshot waits on apps/web). (2) After next Fly deploy, smoke the SSE stream against real prod — current prod runs the broken pre-fix notify.*
- [x] **Phase 13 — Demo mode.** *Done 2026-06-02.* `POST /demo/login` (read-only session on seeded Demo Corp, no signup) + `GET /demo/quota`. **Free-taste quota:** demo visitors get `demo_free_runs` (default 5) hosted-key single runs per client IP per day, then a 402 prompts BYOK (`X-Provider-Key`); BYOK skips the quota. Counter in new `demo_usage` table (migration `0007`), keyed on an HMAC of the client IP (no raw PII at rest). `services/demo.py` = `free_runs_remaining` + atomic `record_free_run` (ON CONFLICT upsert). **Read-only enforcement:** new `require_writer` gate (owner/member) swapped onto every mutating route — prompts create/patch/delete + version create, eval suite/case/run, api-keys create/delete. Single-run route is the one intentional exception (demo + free quota / BYOK). **slowapi wired** (first use): `core/ratelimit.py` limiter + forwarded-IP-aware `client_ip` key func (Fly-Client-IP → X-Forwarded-For → peer); `/demo/login` limited to `demo_rate_limit` (default 5/min); `main.py` registers limiter + 429 handler; conftest resets it per test. **Lean refresh-token reaper:** `services/maintenance.reap_expired_refresh_tokens` (hard-deletes tokens past `refresh_retention_days`, default 90), wired as a periodic task in the worker (`_reaper_forever`, every `refresh_reaper_interval_hours`). 7 new tests (6 demo-flow e2e: login, 503-unseeded, read-only 403s, free-runs-then-402-then-BYOK, quota endpoint, login rate-limit 429; 1 reaper integration). **Deferred:** refresh-token `user_agent`/`ip_address` forensics columns (sign-out-everywhere) — not needed for demo mode; TODO re-tagged in model.
- [x] **Phase 14 — Public share.** *Done 2026-06-02.* `ShareToken` model (migration `0008`) — polymorphic `(resource_type ∈ {prompt, eval_batch}, resource_id)`, token stored as HMAC (plaintext returned once at create), optional `expires_at`, revocable. `services`-free: logic in `api/v1/share.py`. Owner CRUD: `POST /shares` (writer; verifies the resource is in-org before minting), `GET /shares` (tenant list), `DELETE /shares/{id}` (revoke). Public (no auth): `GET /public/share/{token}` — hashes token, rejects revoked/expired, returns minimal projection (prompt: name + latest version; eval_batch: suite name + status + pass_rate + per-case results). `generate_share_token()` added to `core/security.py`. 10 tests: 7 e2e (prompt + eval-batch public views, revoked→404, expired→404, cross-org create→404, create-requires-auth→401, unknown-token→404) + 3 tenancy (list-excludes, revoke-cross-org→404, owner-can-revoke).
- [x] **Phase 15 — Seed.** *Done 2026-06-02.* `promptforge_api/seed.py` (in-package so it's testable/typed/covered; runnable via `python -m promptforge_api.seed`). `seed_demo(session)` is fully idempotent — every section get-or-create keyed on a natural identifier. Seeds: demo user (unusable random password; `/demo/login` only) + Demo Corp + DEMO membership; 3 prompts (Support Reply Drafter w/ v1+v2, SQL Explainer, Release Notes Writer); 4 canned runs incl. one failed (error column populated); eval suite "Support Reply Quality" + 3 contains-cases + a DONE batch with 2/3 pass-rate results; 2 stable public share tokens (`DEMO_PROMPT_SHARE_TOKEN`, `DEMO_EVAL_SHARE_TOKEN` constants). 3 tests (1 integration idempotency+dataset, 2 e2e: seed→demo-login + both public shares resolve, seed-twice safe). *Note: wiring seed into the Fly release_command is Phase 16.*
- [x] **Phase 16 — Deploy + smoke.** *Done — prod smoke green 2026-06-03 (8/8, see "Prod verified" near the top).* structlog wired in `core/logging.py` (JSON in prod / console at DEBUG; request id in contextvars + `X-Request-ID` header) via a **raw-ASGI** `RequestContextMiddleware` (chosen over BaseHTTPMiddleware so it doesn't buffer the SSE stream), configured in the `main.py` lifespan. OTel deferred (Shawn's call): documented gated enable-point in lifespan, zero deps. `fly.toml` release_command → `sh -c 'alembic upgrade head && python -m promptforge_api.seed'` (Fly gives no guaranteed shell, so explicit `sh -c`). DEPLOY.md rewritten as the prod runbook (Neon direct endpoint, `PF_OPENAI_API_KEY` required for demo free runs, `FLY_API_TOKEN` GH secret for the existing CD `deploy` job in api.yml, full smoke). 3 e2e tests for the request-id contract. **Shawn to run:** `fly deploy` (or push to main) → run DEPLOY.md smoke (health/openapi/docs, demo-login, read-only 403, both public shares, X-Request-ID). Also still open from Phase 12: prod SSE smoke now that the notify fix will be deployed.
- [x] **Phase 17 — README + DECISIONS polish.** *Done 2026-06-03.* Root README rewritten for hiring teams: live API/docs URL + the two seeded public-share links (prompt + eval report), "what this demonstrates" (tenancy/404, refresh rotation, eval engine + SSE, one-DB queue, demo-as-product, operability), ASCII architecture, tech-stack table, repo layout, local-dev + quality commands, status, CI badge — all in-repo links (dropped the out-of-repo `../PLAN.md`). apps/api README expanded with the endpoint map + live URL. Added the missing `LICENSE` (MIT, "Shawn Werbin" per pyproject). DECISIONS already comprehensive (35+ entries) — left as-is to avoid AI-tell uniformity. **Shawn-gated:** `/docs` screenshot + Loom link in README; confirm deploy live; prod smoke.

## apps/ragent — 14 / 14 ✅ (feature-complete; `fly deploy` + prod smoke Shawn-gated)

Plan in `PLAN.md` §6. Shares the platform Postgres + the HS256 JWT secret with apps/api; fetches its system prompt from apps/api at runtime. Depends on apps/api's stable `/prompts/{id}/versions/{id}` GET for that live prompt fetch (later phase).

**Schema-ownership decision (locked Phase 2):** ragent **owns its domain models** (`promptforge_ragent/models/` — corpora/documents/chunks/conversations/messages), apps/api **owns the single migration history** that creates the tables. These are different responsibilities (domain vs. infra-migrator) and live apart cleanly. The migration is **hand-authored in apps/api/alembic** (autogenerate can't see ragent's models, and pgvector/ivfflat DDL is hand-written regardless); apps/api's alembic env got an `include_object` guard so a future api autogenerate never proposes dropping ragent's tables. See DECISIONS "Why ragent owns its models but apps/api owns the migration".

- [x] **Phase 1 — Bootstrap.** *Done 2026-06-04.* Scaffolded `apps/ragent/` + the `promptforge_ragent` package: pyproject (uv + hatchling, ruff/mypy-strict/pytest config mirrored from apps/api), FastAPI app (`main.create_app`) with `/health` returning `{status, version}`. `core/config.py` reuses the **shared `PF_` env prefix** (so `PF_DATABASE_URL` + `PF_JWT_SECRET` are the same secret values as apps/api) and carries the same `async_database_url()` DSN normalizer; adds ragent-specific `api_base_url` (+ `system_prompt_cache_seconds`) for the live system-prompt fetch. `core/logging.py` is the same structlog setup + **raw-ASGI `RequestContextMiddleware`** (raw on purpose — ragent's coming SSE chat would be buffered by BaseHTTPMiddleware, same reason as the api's eval stream). `Dockerfile` (python 3.12-slim + uv, **no alembic dir** — the shared DB's migration history is owned by apps/api), `fly.toml` (`promptforge-ragent`, **single `web` process**; ingest-worker process added in Phase 4; **no `release_command`** since ragent never migrates), `.env.example`, `.dockerignore`, README. CI skeleton `.github/workflows/ragent.yml` (path-filtered lint + format + mypy-strict + unit on py 3.11/3.12). **Verified locally:** 5 unit tests pass (health smoke incl. `X-Request-ID` echo + 4 DSN-normalizer cases), ruff + ruff format + mypy --strict clean. *Note:* unit tests import `create_app` **inside** the test bodies (after the `base_env` fixture sets env) because `main` builds `app = create_app()` at import — same pattern as apps/api.
- [x] **Phase 2 — DB models.** *Done 2026-06-05.* Five ragent models in `promptforge_ragent/models/` on a `Base` mirroring apps/api's verbatim (same `NAMING_CONVENTION` so constraint names match the shared schema): **Corpus** (per-org, `slug` unique per org, `EmbeddingModel` enum pinning 1536-d OpenAI vs 384-d bge), **Document** (corpus child, ingest lifecycle `status`/`error`, `content_type`, `byte_size` for the upload cap), **Chunk** (two nullable `Vector` cols `embedding_1536`/`embedding_384` + **partial ivfflat indexes** `WHERE embedding_xxxx IS NOT NULL`, `lists=100`, `vector_cosine_ops`; BM25 stays in-process so no tsvector col), **Conversation** (nullable `corpus_id`), **Message** (`MessageRole` enum, `citations`/`tool_calls` JSONB for the source drawer + tool chips). `org_id` denormalized onto documents/chunks/messages for direct tenant scoping (same read-perf tradeoff as `Run.org_id`). Added `core/db.py` (lazy async engine/session, mirrors api). Deps added: `sqlalchemy[asyncio]`, `asyncpg`, `pgvector`; dev: `testcontainers[postgres]`. **Migration `20260605_0009` authored in apps/api/alembic** (hand-written; vector cols + ivfflat indexes emitted as **raw SQL** so apps/api needs no `pgvector` runtime dep). apps/api `alembic/env.py` got an `_include_object` guard (autogenerate ignores tables not in api's metadata → never drops ragent's). **Test-image swap:** apps/api testcontainers + `infra/compose.yml` moved `postgres:17-alpine` → `pgvector/pgvector:pg17` (the migration now needs the `vector` extension; Neon prod already has it). **Verified:** ragent 5 unit + 2 integration (pgvector container: 1536-d vector round-trip + cosine-distance ordering, conversation/message graph w/ JSONB) pass, ruff+format+mypy clean; **apps/api full suite 223 passed** with 0009 applied on the pgvector image (migration applies clean end-to-end). *Note:* ragent integration conftest registers minimal stub `orgs`/`users` tables in `Base.metadata` so `create_all` can resolve ragent's FKs without importing the api package; real tables come from api's migrations everywhere else.
- [x] **Phase 3 — Ingest pipeline.** *Done 2026-06-05.* Four composable services in `promptforge_ragent/services/` + an orchestrator: **`parsing.extract_text(content_type, data)`** (markdown → markdown-it render → selectolax/lexbor flatten, sharing the HTML path so `**bold**` doesn't pollute embeddings; PDF via pypdf page-join; text passthrough; bad bytes decoded `errors="replace"`). **`chunking.chunk_text(text, target_tokens=512, overlap_tokens=64)`** — fixed-size sliding **token** windows via tiktoken `cl100k_base` (step = target−overlap), returns `TextChunk(ordinal, text, token_count)`; *considered* paragraph-aware packing, chose deterministic windows + left a note that semantic splitting is the documented improvement. **`embeddings.embed_texts(model, texts)`** — routes on the corpus's `EmbeddingModel`: OpenAI `text-embedding-3-small` (1536-d) via `litellm.aembedding` with dim-validation; **local bge path raises a `TODO(phase-12)` seam** (avoids pulling torch/sentence-transformers now). **`ingest.ingest_document(session, document, data)`** — parse→chunk→embed→persist; writes the vector into the 1536/384 column matching the corpus, **idempotent re-ingest** (deletes prior chunks first), records durable terminal state (READY w/ chunks, or FAILED + error captured) and does **not** re-raise — mirrors api's eval-runner; `TODO(phase-4)` to classify transient provider errors as retriable in the worker. Deps added: `litellm`, `tiktoken`, `markdown-it-py`, `pypdf`, `selectolax`. **Verified:** ragent **24 tests** (19 unit: parsing per type incl. PDF-reader-mocked + unknown-type, chunking windows/overlap/empty/bad-overlap, embedding routing incl. dim-mismatch + provider-error-wrap + bge-seam; 5 integration: 1536-d vector round-trip + cosine ordering [models], ingest persists-with-vector + idempotent re-ingest + parse-failure→FAILED), ruff+format+mypy --strict clean. No apps/api changes this phase.
- [x] **Phase 4 — Ingest worker.** *Done 2026-06-05.* Queue-driven worker that drives `ingest_document` off the **shared `jobs` table** (apps/api owns it via migration 0004; ragent enqueues `kind="ingest_document"`, api's eval worker only claims `eval_case` → they coexist). `core/queue.py` — a trimmed client (raw `text()` SQL, **no ORM Job model**): enqueue + `pg_notify`, CTE `FOR UPDATE SKIP LOCKED` claim, `ClaimedJob` async-ctx-manager (ack on clean exit / requeue-with-backoff on raise / terminal `failed` after max_attempts). `workers/ingest_worker.py` — `_consume_forever` loop + graceful SIGINT/SIGTERM, `_handle_ingest` loads the document + its bytes and runs the pipeline, `enqueue_ingest` helper for the upload/seed paths. **Transient vs terminal:** `embeddings` now raises `RetriableEmbeddingError` for litellm's transient set (APIConnectionError/Timeout/RateLimitError/APIError); `ingest_document` re-raises it (worker requeues with backoff) instead of marking FAILED; the worker marks the document FAILED only on the **final** attempt (`ClaimedJob.is_last_attempt`) so it never hangs in INGESTING. **`documents.raw_content` BYTEA** added (migration `0010` in apps/api) — the detached worker reads source bytes from the row; retained for re-ingest. `fly.toml` gained the **`ingest-worker` process** (second scale dial, scale-to-0 when idle). **Two fixes found via tests:** (1) requeue backoff now uses the **DB clock** (`now() + make_interval`) not the host clock — corrects for Docker/WSL2 container-clock drift that made host-computed `run_after` already-past; (2) `_run_one` swallows+logs after the job ctx-manager so a re-raised transient (which __aexit__ turns into a requeue) doesn't kill the consume loop. **Verified:** ragent **31 tests** (+7: queue enqueue/claim/ack, requeue-with-backoff, terminal-fail-after-max, SKIP-LOCKED no-double-claim; worker ingests-queued-doc, transient-requeues-without-failing, transient-on-last-attempt→FAILED) green; **apps/api full suite 223 passed with 0009+0010 applied**. *Test infra:* new `committed_db` fixture (real committing session factory + truncate, since queue/worker commit across sessions) and a stub `jobs` table in the test schema (the real one is api-owned).
- [x] **Phase 5 — Hybrid retrieval.** *Done 2026-06-06.* `services/retrieval.py` — `hybrid_search(session, corpus, query, *, top_n=10, k_dense=20, k_sparse=20, rrf_k=60)`: **dense** (embed query via per-corpus model → pgvector `cosine_distance` order on the populated 1536/384 column, scoped `corpus_id` + `org_id` + `col IS NOT NULL`) + **sparse** (BM25 in-process via `rank-bm25` over the corpus's chunk texts, keeps only positive-scoring/lexically-matching) → **RRF fusion** (`reciprocal_rank_fusion`, 1-based rank, `1/(k+rank)`, k=60). Returns `RetrievedChunk(chunk_id, document_id, corpus_id, ordinal, content, score)` in fused order. RRF uses ranks only → no score calibration between cosine + BM25. BM25 index rebuilt per query (demo scale; per-corpus cache w/ ingest-invalidation is the documented optimization). Dep: `rank-bm25`. **Verified:** ragent **39 tests** (+8: 5 unit — tokenizer, RRF empty/single-list/both-lists-reward/lower-rank-still-counts; 3 integration — **lexical-only match (dense-far) lifted above a dense-equal no-keyword chunk via BM25** [the core hybrid property], empty-query→[], corpus/org scoping excludes other corpus), ruff+format+mypy --strict clean. No apps/api changes this phase.
- [x] **Phase 6 — Optional rerank.** *Done 2026-06-07.* `services/rerank.py` — `async rerank(query, chunks, *, top_n)` reorders `RetrievedChunk`s by a **cross-encoder** (bge-reranker-base) when `PF_RERANK_ENABLED=true`, else **passthrough** (the RRF order stands; still truncates to `top_n`). **Off by default** + the heavy dep (`sentence-transformers`/torch) lives in an **optional `rerank` extra** that CI and the default Docker image never install — same senior move as OTel-deferred + the bge Phase-12 seam. When enabled: model lazy-imported + cached (module singleton), inference offloaded via `asyncio.to_thread` so it doesn't block the loop; each returned chunk's `score` becomes its cross-encoder score. Config: `rerank_enabled` (False), `rerank_model` (`BAAI/bge-reranker-base`). Verified `CrossEncoder(...).predict([(query, doc), …])` API against docs (untestable in CI by design). **CI fix:** `ragent.yml` install switched `uv sync --all-extras` → `--extra dev` so CI doesn't pull torch. **Verified:** ragent **43 tests** (+4 unit: disabled-passthrough, disabled-still-truncates, enabled-reorders-by-CE-score [scorer mocked, no torch], enabled-single-chunk-skips-scorer); confirmed mypy --strict + full suite green **without** sentence-transformers installed (CI-equivalent). Wiring rerank into the agent retrieval flow is Phase 7/8. No apps/api changes this phase.
- [x] **Phase 7 — Agent tools.** *Done 2026-06-07.* New `agent/` package. `agent/tools.py` — the 3 ReAct tools as litellm function-calling schemas (`TOOL_SCHEMAS`) + handlers + an `execute_tool` dispatcher, all bound to a `ToolContext(session, corpus)` and scoped to that corpus + org: **`search_docs`** (`hybrid_search` candidate pool of 20 → `rerank` narrows to `top_k`, returns ranked `{chunk_id, document_title, ordinal, snippet, score}`), **`fetch_passage`** (full chunk content by id; rejects bad/missing/out-of-corpus ids), **`cite_sources`** (validates ids against the corpus, returns citation projections in the model's requested order, drops foreign ids). **Handlers return `{"error": ...}` dicts instead of raising** — the ReAct loop feeds errors back so the model self-corrects (raising would abort the turn). Snippets truncate at 280 chars; `search_docs` wires retrieval + the (default-off) rerank together for the first time. **Verified:** ragent **53 tests** (+10: 4 unit — schema shape, `_snippet` truncation, `_parse_uuid`, unknown-tool→error; 6 integration — search returns ranked results + requires query, fetch returns full content + rejects bad/missing/cross-corpus ids, cite preserves order + drops foreign ids + validates input), ruff+format+mypy --strict clean. ReAct **loop** that calls these via litellm is Phase 8. No apps/api changes this phase.
- [x] **Phase 8 — Agent loop.** *Done 2026-06-07.* **`agent/loop.py`** — `run_agent` async-generator ReAct loop: litellm function-calling with `TOOL_SCHEMAS` → parse `tool_calls` → `execute_tool` → append results → loop, yielding small JSON events (`tool_call`/`tool_result`/`answer`) so Phase 9 SSE just maps them. **Two safety rails:** `agent_max_iterations` (default 6) cap + a **circuit breaker** (`_MAX_DUPLICATE_CALLS=2`) on repeated identical tool+args (json-signature set); when either fires, one **tool-less completion forces a coherent final answer** (`truncated: True`) instead of dumping a half-finished transcript. Citations from `cite_sources` ride out on the `answer` event; malformed tool-call JSON → `{}` args (no crash). **`services/system_prompt.py`** — `get_system_prompt()` fetches the agent prompt **live from apps/api** (`GET /api/v1/versions/{id}`) authenticated by a **service JWT minted from the shared HS256 secret** for a configured service principal; TTL-cached (`system_prompt_cache_seconds`); **built-in `DEFAULT_SYSTEM_PROMPT` fallback** when unconfigured or on fetch failure (success + unconfigured-default cached; *failure not cached* → recovers next request). Config added: `system_prompt_version_id`, `service_org_id`, `service_user_id`, `agent_model` (gpt-4o-mini), `agent_max_iterations`. **Seam:** the seed/deploy (Phase 11) sets `system_prompt_version_id` + the service principal ids; until then the default prompt is used. **Verified:** ragent **63 tests** (+10: 6 unit system-prompt — default/live-fetch+cache/failed-fetch-not-cached/service-token-mint+none; 5 unit loop — happy search→cite→answer, circuit-breaker→forced-answer, max-iter→forced-answer, malformed-args-tolerated; 1 integration — **loop drives the REAL tools against Postgres → citations from actual chunk rows**, litellm scripted), ruff+format+mypy --strict clean. *Note: Docker Desktop was down mid-session; started it + verified all 22 integration tests green.* litellm tool-calling message shape (assistant `tool_calls` + `role:"tool"` results) built explicitly per the OpenAI protocol. No apps/api changes this phase.
- [x] **Phase 9 — Chat SSE.** *Done 2026-06-07.* ragent's first HTTP surface. **Auth:** `core/security.py` (`decode_access_token` — validates the **shared-HS256** token apps/api issues, `typ=access`) + `core/deps.py` (`Principal` + `get_principal` bearer dep). **Route** `POST /api/v1/chat` (`api/v1/chat.py`, wired via `api/v1/__init__.py` → `main.include_router`): authenticate → resolve corpus (org-scoped, by id/slug, or from an existing conversation) → load/create `Conversation` → load prior history → **persist user `Message` + commit** → `get_system_prompt()` → `run_agent` → **stream the loop's events over SSE** (`conversation` id event, then `tool_call`/`tool_result`/`answer`, then `done`; `error` event on bad corpus/conversation) → **persist assistant `Message`** (content + `citations` + `tool_calls` trail) + commit. **BYOK:** `X-Provider-Key` header → `api_key` passed to the loop (hosted key otherwise). **SSE generator owns its own session** via `get_session_factory()` (the body runs after the endpoint returns the `EventSourceResponse`, so a request-scoped session would be closed — same reason as apps/api's eval stream). Added `sse-starlette` dep. **Verified:** ragent **72 tests** (+9: 6 unit auth — decode valid/bad-sig/non-access-typ, get_principal happy/missing/invalid; 3 e2e — **full HTTP chat: SSE stream `conversation`→`tool_call`→`answer`→`done` + user/assistant Messages persisted with real citations** [litellm + embedding mocked, real tools/DB], auth-required→401, unknown-corpus→`error` event), ruff+format+mypy --strict clean. *Test infra:* new `app_client` fixture (ASGI client on `committed_db`'s env/engine). *SSE parse gotcha: sse-starlette 3.x uses CRLF separators.* No apps/api changes this phase.
- [x] **Phase 10 — Corpora API + upload.** *Done 2026-06-07.* `api/v1/corpora.py` (wired into the v1 router): **`GET /corpora`** (org-scoped list w/ `document_count` via outerjoin+group_by), **`POST /corpora`** (create; slug validated `^[a-z0-9][a-z0-9-]*$`, unique-per-org → 409 on conflict), **`POST /corpora/{id}/documents`** (multipart upload → `Document(raw_content=bytes, status=PENDING)` + commit → **`enqueue_ingest`** on the shared queue → returns immediately with `pending`; the Phase-4 worker does parse→chunk→embed async), **`GET /corpora/{id}/documents`** (list w/ status/error for ingest progress). **Reads open to any principal (demo included); writes gated to writer roles** (`{owner, member}` — demo browses + chats seeded corpora but can't run up embedding cost via upload). **Caps:** 5 MB/file → 413, 50 MB/corpus (sum existing `byte_size` + new) → 413; content type detected by extension (.md/.markdown/.pdf/.html/.htm/.txt) → 415 on unknown. Config: `max_file_bytes`, `max_corpus_bytes`. Deps: `python-multipart`. Used the **new `HTTP_413_CONTENT_TOO_LARGE`** constant (not the deprecated `_REQUEST_ENTITY_TOO_LARGE`) per [feedback-latest-lts]. **Verified:** ragent **81 tests** (+9: 2 unit content-type detection; 7 e2e — create+list, demo-create→403, duplicate-slug→409, upload→201 pending + document persisted + **ingest job enqueued** (jobs row `payload->>'document_id'`), unknown-type→415, file-size-cap→413, cross-org corpus→404), ruff+format+mypy --strict clean, no warnings. No apps/api changes this phase.
- [x] **Phase 11 — Seed.** *Done 2026-06-08.* **Decision locked** (see DECISIONS): apps/api **creates** the agent's system prompt (it owns prompts) and ragent **resolves it from the shared DB by natural key + fetches the body over HTTP** — closes the Phase-8 seam without fragile id-wiring. **apps/api change:** seed adds a `"RAG Agent System Prompt"` prompt+v1 in Demo Corp (`RAGENT_SYSTEM_PROMPT_NAME`); seed test count 3→4 prompts. **ragent `services/platform.py`** — read-only shared-DB discovery (raw SQL, ragent doesn't model these): `resolve_demo_principal` (demo org by slug + a member user) + `resolve_prompt_version_id` (latest version of a prompt by name). **`system_prompt.py` refactored** off the Phase-8 env ids to DB-resolution: `get_system_prompt(session)` resolves principal+version from the DB → mints the service JWT → fetches the body via HTTP (cache + default fallback unchanged). Config: dropped `system_prompt_version_id`/`service_org_id`/`service_user_id`, added `demo_org_slug` + `system_prompt_name`. **`seed.py` + `seed_data.py`** — 3 demo corpora (`promptforge-docs` self-referential, `fastapi-docs`, `arxiv-ml-abstracts`; 7 short docs) seeded into Demo Corp by **running real `ingest_document`** (parse→chunk→embed); idempotent (corpus by (org,slug), doc by (corpus,title), ingest only if 0 chunks); `python -m promptforge_ragent.seed` resolves the demo org first (no-ops + warns if apps/api unseeded). **Verified:** ragent **86 tests** (+5: 5 unit system-prompt rewritten — resolved-fetch+cache, default-when-unseeded ×2, failed-fetch-not-cached, token-mint; +5 integration — resolver finds org/latest-version + missing-org→None + get_system_prompt resolves-then-fetches; seed creates+ingests-to-READY + idempotent [embeddings mocked]); **apps/api full suite 223 passed** (seed→4 prompts). *Test infra:* expanded conftest stubs (orgs+slug/name, memberships, prompts, prompt_versions) for the cross-domain resolver. *Deploy wiring (ragent seed in a Fly release_command + hosted embedding key secret) is Phase 13.*
- [x] **Phase 12 — Per-corpus embedding routing.** *Done 2026-06-08.* Implemented the **local bge-small-en-v1.5 (384-d)** backend in `embeddings.py` — the Phase-3 seam — finishing per-corpus routing (column selection was already wired in Phase 2/3; this makes the BGE branch produce real vectors instead of raising). `_embed_bge` lazy-loads + caches a `SentenceTransformer` (module singleton), runs inference via `asyncio.to_thread` (CPU-bound), `normalize_embeddings=True`, and **dim-validates 384**. **bge is a query-instruction model**, so `embed_texts(model, texts, *, is_query=False)` gained the flag: queries get the `"Represent this sentence for searching relevant passages: "` prefix, passages don't (verified against BAAI docs); the OpenAI path ignores it. `retrieval.hybrid_search` now passes `is_query=True`; ingest stays `is_query=False`. **Off by default** — torch lives in a new optional **`local-embeddings` extra** (CI/image never install it), only loaded when a corpus is actually pinned to bge (seeded corpora are OpenAI). Config: `embedding_bge_model`. **Verified:** ragent **89 tests** (+5 unit: bge passages no-instruction + 384-d + normalize, bge query prepends instruction, bge dim-mismatch→error, OpenAI ignores is_query — all with `SentenceTransformer` mocked, **no torch**; fixed 4 retrieval/agent/chat embed-fakes to accept the new kwarg). ruff+format+mypy --strict clean **without** sentence-transformers installed (CI-equivalent). No apps/api changes this phase.
- [x] **Phase 13 — Deploy + smoke.** *Done 2026-06-08 (code-complete; `fly deploy` + prod smoke are Shawn's, like apps/api).* **Demo cost control** (closes the Phase-9 follow-up; **answers Shawn's VPN concern**): free hosted-key agent turns then BYOK, bounded by **two daily caps** — **per-IP** (`HMAC(ip)`, UX/casual abuse) **+ a global cap** (one sentinel row, the real backstop against IP/VPN rotation — reliable VPN *detection* needs a paid reputation service + is an arms race; a global ceiling makes rotation pointless). `DemoUsage` model (`ragent_demo_usage`, migration **`0011`** in apps/api) + `services/demo_quota.py` (forwarded-IP-aware `client_ip` → `Fly-Client-IP`/`X-Forwarded-For`, `hmac_ip`, `free_turns_remaining` = min(per-IP, global), atomic `ON CONFLICT` `record_free_turn`). Chat endpoint: demo + no BYOK + 0 remaining → **402 before the stream opens**; the turn is recorded only when it produces an answer. Config: `demo_free_turns_per_ip` (3), `demo_free_turns_global` (200). **Deploy infra:** `ragent.yml` gained a **full-suite `tests` job** (testcontainers pgvector; all external calls mocked → no secrets) + a **`deploy` job** (Fly on green `main`, needs lint+tests). `docs/DEPLOY-RAGENT.md` runbook (shared Neon + **identical `PF_JWT_SECRET`**, `PF_API_BASE_URL`, hosted key; deploy **after apps/api**; **manual one-time seed** `python -m promptforge_ragent.seed` — kept out of `release_command` so an OpenAI blip can't block deploy; worker scale; SSE smoke). structlog/request-id already wired (Phase 1). **Verified:** ragent **93 tests** (+4 e2e quota: demo free-turns→402, BYOK skips quota, **global cap stops IP rotation**, member unlimited); **apps/api 223 passed** (migration 0011 applies) + ruff clean. No demo-cost slowapi rate-limit added (the caps bound hosted cost; BYOK/real users spend their own) — noted as optional hardening.
- [x] **Phase 14 — README + docs polish.** *Done 2026-06-08.* Rewrote `apps/ragent/README.md` from the Phase-1 bootstrap stub into the **hiring-team headline** (mirrors apps/api's README): one-liner + live URL (deploy pending), **what-it-demonstrates** (hybrid retrieval + RRF, ReAct loop + safety rails + citations, live system-prompt fetch, shared-DB queue + model/migration ownership split, per-IP+global demo caps, opt-in heavy deps), agent-flow architecture diagram, endpoint + stack tables, local-dev + quality commands, docs links. **Root `README.md`:** added the ragent CI badge, a "RAG agent that consumes the platform" bullet to *what this demonstrates*, updated repo-layout (ragent **feature-complete**; web → Next.js 16) + status, bumped the test-count line to 300+. DECISIONS left as-is — already comprehensive (40+ entries across api + ragent Phases 2–13) and deliberately human-voiced (avoiding AI-tell uniformity per [feedback-ai-use-style]). **apps/ragent is now feature-complete (14/14).** Docs-only; ruff/mypy unaffected. **Shawn-gated:** `fly deploy` promptforge-ragent (after apps/api), the one-time corpora seed, prod smoke (DEPLOY-RAGENT.md).

**Deploy fixes — found during the first prod deploy (2026-06-09):**
1. **OOM on 512 MB → bumped Fly machines to 1 GB.** litellm is ~200 MB RSS and *both* processes load it (the web/chat agent + the worker's embed-on-ingest); the manual `python -m promptforge_ragent.seed` (also loads litellm) running on the worker machine pushed 512 MB over the limit → kernel OOM-killed it + rebooted. `fly.toml` both `[[vm]] memory_mb` 512 → **1024** (still auto-scale-to-0 when idle). Would have hit a real upload-ingest too.
2. **`NoReferencedTableError` on first insert → declared api-owned FK tables in production.** ragent's models FK to apps/api's `orgs`/`users`; SQLAlchemy must resolve those FK targets in `Base.metadata` to flush an insert, but production ragent had no such declaration → every Corpus/Conversation/etc. insert (and the seed) raised it. **The e2e tests passed only because *conftest* registered stub `orgs`/`users`, masking the gap.** Fix: `models/_external.py` declares partial `orgs`(id,slug,name)/`users`(id) on `Base.metadata` (imported for side effect); ragent never `create_all`'s them (api's migrations own the real tables). Conftest stubs removed → tests now exercise the production declaration. **Lesson: a test fixture must not supply schema that production needs — it hides prod-only failures.** 93 tests green.

*Operational gotchas captured for next time:* (a) **Fly ssh sessions don't inherit the image `PATH`** — use `/app/.venv/bin/python`, not bare `python` (system python lacks the deps). (b) **Neon's copied connection string** uses the `-pooler` host + `&channel_binding=require`; asyncpg needs the **direct** host (pooler breaks its prepared-statement cache) and chokes on `channel_binding` (not an asyncpg kwarg) — use the direct host + only `?sslmode=require`. (c) set DB URLs **single-quoted** in `fly secrets set` so the shell doesn't eat `$`/special chars in the password.

## apps/web — 9 / 16  (resumed; apps/ragent now feature-complete)

> **Resumed 2026-06-09 after apps/ragent shipped (14/14, live at promptforge-ragent.fly.dev).** Core web usable + ragent chat works end-to-end. **Remaining:** 10 (ragent corpora/upload UI), 11 (Settings + API keys), 12 (public-share `/share/[token]` view), 13 (Playwright E2E), 14 (visual polish), 15 (Vercel deploy), 16 (web README) — all independent. **Web not yet deployed** (Vercel deferred). **Resolved:** apps/api `GET /runs` is live in prod (dashboard Runs tile populates). **Still open:** regen `apps/web/lib/api/schema.ts` from the deployed API (folds in `/runs`); ragent has no web schema-gen — its small surface is hand-typed in `models.ts`.

Plan in `PLAN.md` §7. **Stack note:** built on **Next.js 16** (not 15 — ecosystem moved by 2026-06; App Router reasoning unchanged) and **npm** (not pnpm — single JS app, not a JS monorepo; api + ragent are Python/uv, so pnpm's workspace edge doesn't apply; Shawn knows npm). React 19, TS strict, Tailwind v4, ESLint flat config. Dev points at the **local** API (`NEXT_PUBLIC_API_URL=http://localhost:8000`); API types will be generated from the local API via `openapi-typescript`.

- [x] **Phase 1 — Bootstrap.** *Done 2026-06-03.* Scaffolded via `create-next-app` (correct-for-v16 baseline: Tailwind v4 CSS-first, ESLint 10 flat config, Next 16 tsconfig — fiddly version-specific bits not worth hand-rolling), then customized: removed generator `AGENTS.md`/`CLAUDE.md` cruft; `package.json` name → `promptforge-web` + `typecheck` + `gen:api` scripts; **dark-by-default** (class-based `.dark` on `<html>`, `@custom-variant dark` in globals, no toggle); real metadata; PromptForge placeholder home (links to live API `/docs`); `.env.example` (`NEXT_PUBLIC_API_URL`); README (stack + dev loop + scripts); added `openapi-typescript` dep + `gen:api` script (not run yet — wired in the API-client phase). CI: `.github/workflows/web.yml` (path-filtered lint + typecheck + build on Node 24; Vercel handles deploy). **Verified:** `npm run lint`, `npm run typecheck`, `npm run build` all green (home prerenders static).
- [x] **Phase 2 — Auth shell (BFF).** *Done 2026-06-03.* **Decision: BFF over direct cross-origin** (supersedes the old "in-memory access token" note — browsers are phasing out third-party cookies, so a cross-site refresh cookie is fragile). The browser only ever talks to the Next origin; Next route handlers (`app/api/auth/{login,signup,logout}`) proxy to the API. The session (API access + refresh tokens + small profile) is sealed as a **JWE** (`jose`, key = SHA-256 of `WEB_SESSION_SECRET`) in an **httpOnly, same-origin** cookie (`pf_session`) — **tokens never reach browser JS**. `lib/api/server.ts`: `authenticate()` (extracts the API's `pf_refresh` from its `Set-Cookie` server-side), `apiFetch()` (attaches access token, refresh-on-401 + rotate + re-seal — for use in route handlers where cookies are writable; RSC-render refresh deferred to later phases), `apiLogout()`. API types generated to `lib/api/schema.ts` (`openapi-typescript`, used to type the auth responses). `lib/auth-context.tsx` client provider (profile only, no tokens) hydrated server-side in the root layout. Login/signup pages with **react-hook-form + zod**; `/dashboard` protected; `proxy.ts` (Next 16 renamed `middleware`→`proxy`) presence-gates `/dashboard`. **Runtime-verified against the live API:** signup→200 (profile only, `pf_session` HttpOnly + opaque JWE), logout→200, bad-login→401. lint + typecheck + build green.
- [x] **Phase 3 — Layout + sidebar (app shell).** *Done 2026-06-03.* `app/(app)/layout.tsx` — server-component shell that guards the whole `(app)` group (`readSession()` → `redirect("/login")` if absent; authoritative gate behind proxy.ts's edge redirect). Composes `components/app-shell/sidebar.tsx` (client, `usePathname` active-link highlight; nav: Dashboard/Prompts/Evals/Settings) + `topbar.tsx` (client, `useAuth` → workspace slug + email + role pill + logout). Dashboard rebuilt as a server component with placeholder KPI tiles (logout moved to the topbar); placeholder pages for `/prompts`, `/evals`, `/settings` via a shared `Placeholder` component so nav doesn't 404. proxy.ts matcher broadened to all four app routes. lint + typecheck + build green.
- [x] **Phase 4 — Landing + Try Demo.** *Done 2026-06-04.* Replaced the placeholder home with a real marketing landing (hero + tagline, feature cards: versioned prompts / 4-judge eval suites / live SSE results, and "see it without an account" links to the live public eval report + API docs). **Try Demo:** new BFF route `app/api/auth/demo` → `authenticateDemo()` (POSTs API `/demo/login`, seals the demo session like login/signup); `sessionFrom` refactored to a shared `AuthLike` shape so login/signup/demo reuse it. Client `TryDemoButton` + `tryDemo()` on the auth context → drops the visitor into `/dashboard`. **Runtime-verified vs live API:** Try Demo → 200, `role=demo`, `orgSlug=demo-corp`, sealed httpOnly cookie. lint + typecheck + build green.
- [x] **Phase 5 — API client + SSE client.** *Done 2026-06-04.* Generic BFF data proxy `app/api/pf/[...path]` (GET/POST/PATCH/DELETE → `apiFetch` with token + refresh-on-401; restricted to `/api/v1/*`, 204 handled). SSE proxy `app/api/pf-stream/[...path]` → `apiStream()` (attaches token, refresh-on-401, pipes upstream `text/event-stream` to the browser — needed because EventSource can't set an auth header). Client helpers: `lib/api/client.ts` (`api.get/post/patch/del` → `/api/pf/*`) and `lib/api/sse.ts` (`streamSse()` via fetch + `eventsource-parser` v3, returns an abort fn). Types already generated (`schema.ts`). **Runtime-verified vs live API:** demo login → proxy `GET /api/pf/api/v1/prompts` returns the 3 seeded prompts; demo `POST` through proxy → 403 (read-only enforced). lint + typecheck + build green.
- [x] **Phase 6 — Prompts pages.** *Done 2026-06-04.* First real do-something UI. `/prompts` (list — fetch via `api.get` through the proxy, New-prompt link, empty state), `/prompts/new` (create form: name/description/visibility + Monaco body + variables editor → `api.post` → redirect to detail), `/prompts/[id]` (detail: meta + delete, latest version in read-only Monaco + declared-vars chips, version history list, and an inline "Add new version" form → append-only `POST .../versions`). Shared `components/code-editor.tsx` (lazy `@monaco-editor/react`, `vs-dark`), `components/prompts/variables-editor.tsx`, and `lib/api/models.ts` (aliases over the generated schema). Data fetched client-side through the BFF proxy with the inline-`.then` + refresh-key pattern (React 19's `react-hooks/set-state-in-effect` rule disallows calling a setState helper directly in an effect). **Runtime-verified vs live API** (fresh signup, through the proxy): create→201, list total 1, add version→**v2**, versions count 2, delete→204. lint + typecheck + build green.
- [x] **Phase 7 — Evals pages (live SSE).** *Done 2026-06-04.* The headline screen. `/evals` (suites list), `/evals/new` (create suite + default judge), `/evals/[id]` (suite detail: cases list + add-case form [inputs key→value rows, expected value, judge override] + Run section that picks a prompt, resolves its latest version, and `POST .../run` → navigates to the batch), `/evals/batches/[id]` (**live SSE**: initial GET, then `streamSse` on the batch channel → progress bar + per-case pass/fail/score as `result` events arrive, then refetch full results with judge reasoning on `done`). SSE opened inside the initial-GET `.then` so refresh-key/effect rules stay happy; aborts on unmount. Eval type aliases + `BatchProgressEvent` added to `models.ts`. **Runtime-verified vs live API:** full eval flow through the BFF (signup → prompt → suite → case → run → batch created) and the **BFF SSE proxy streamed `event: open`** through to the client (`result`/`done` need `worker=1`; same event format already proven at the API level). lint + typecheck + build green.
- [x] **Phase 8 — Dashboard.** *Done 2026-06-04.* Replaced the placeholder dashboard with real data: 3 KPI tiles (Prompts total, Eval suites count, Runs total) + a recent-runs table (model, cost, latency, ok/error, time), all fetched through the BFF proxy. **Required an apps/api addition:** `GET /api/v1/runs` (paginated `RunListResponse`, tenant-scoped via `TenantRepository[Run].list/count`, newest-first) — the API only had `GET /runs/{id}`. Added with an e2e test (pagination + cross-org isolation); **full api suite 223 passed, coverage 84.98%**. Web `Run`/`RunList` aliases in `models.ts` (RunListResponse defined manually — added to the API after schema gen). **Verified:** web build/typecheck/lint green; prompts+suites tiles read live demo data through the proxy (proven in phases 5–6). *Follow-ups (Shawn): redeploy apps/api so the live dashboard's Runs tile/table populate (GET /runs 404s on prod until then); regen `apps/web/lib/api/schema.ts` from the updated API to fold in the `/runs` path.* Note: recreating `apps/api/.venv` pulled newer deps (ruff 0.5→0.15) which reformatted 4 untouched test files — included in the api commit.
- [x] **Phase 9 — ragent chat.** *Done 2026-06-09.* Chat UI against the **ragent service** (separate origin, shares the HS256 token). **BFF extended for a 2nd target:** `apiFetch`/`apiStream` gained a `base` param; `lib/api/config.ts` adds `RAGENT_URL` (`NEXT_PUBLIC_RAGENT_URL`, local :8001). Two new proxy routes: `app/api/pf-ragent/[...path]` (JSON → corpora) and `app/api/pf-ragent-chat` (**POST + SSE** — `EventSource` can't POST a body; attaches the session token, forwards 402/401 before the stream). Client: `client.ts` refactored to a `makeClient(prefix)` factory exporting `api` (/api/pf) + `ragent` (/api/pf-ragent); `sse.ts` gained `streamChat()` (POST + `eventsource-parser`). `/chat` page: corpus picker (ragent `GET /corpora`), streaming messages, **tool-call chips** as `tool_call` events arrive, **citations** under each answer, conversation continuity via the `conversation` event. ragent types hand-added to `models.ts` (Corpus/RagentDocument/Citation/ChatMessage). Sidebar + proxy matcher gained `/chat`. **Runtime-verified end-to-end vs LIVE api + ragent:** demo login → corpora list (3 seeded) → chat → SSE `conversation`→`tool_call`/`tool_result`×3→`answer`→`done` with a real **cited** answer. lint + typecheck + build green.
- [ ] **Phase 10 — ragent corpora + upload UI.** (next) corpus create + document upload + ingest-status list.

## Integration + polish — 0 / 3

Root README polish, demo seed validation, end-to-end smoke. After all three apps deploy.

---

## Open TODOs in code

Grep `TODO(phase-` to refresh:

| File | Line | Tag | What |
|---|---|---|---|
| `apps/api/promptforge_api/main.py` | ~22 | deferred | structlog DONE (phase 16); OpenTelemetry deferred — gated enable-point documented in `lifespan`, no deps. |
| `apps/api/promptforge_api/models/api_key.py` | 3 | phase-5+ | Add `scopes` jsonb column for least-privilege keys. |
| `apps/api/promptforge_api/models/refresh_token.py` | 3 | deferred | Add `user_agent` + `ip_address` for forensics + "sign out everywhere" — deferred past phase-13 (not needed for demo mode). |
| `apps/api/promptforge_api/api/v1/auth.py` | ~341 | phase-5+ | Per-key scopes enforcement. |
| `apps/api/promptforge_api/api/v1/prompts.py` | 240 | phase-5+ | Collapse `next_version_number` + insert into one statement (kill race window). |
| `apps/api/tests/e2e/test_prompts_flow.py` | 133 | phase-7+ | Real "private prompt hidden from same-org peer" test once member-invites land. |

## Code health snapshot

- **51 source files.**
- **223 tests pass** (full suite, Docker up — 2026-06-04; coverage 84.98%). Unit + integration (testcontainers models/queue/reaper/seed) + tenancy (api-keys/prompts/shares) + e2e (auth/prompts/runs incl. list/evals incl. SSE + demo mode + share links + seed + request-id).
- ruff + ruff format + mypy --strict clean across all 51 source files.
- **CI fix:** coverage gate moved off the unit-only job onto the full suite (`tests` job, `--cov-fail-under=80`; actual ~85%). Unit-alone coverage had drifted to 62.9% as route code grew (it's covered by e2e), reddening CI.
- **Bug fixed (API key prefix):** prefix was `token_urlsafe` (alphabet has `_`) but parsed via `split('_')` → ~12% of keys parsed wrong and 401'd on lookup (surfaced as a flaky api-key test). Now hex prefix + fixed-length parse; 200-key round-trip regression test.
- **CI workflow:** `.github/workflows/api.yml` — split into unit (3.11/3.12 matrix) + integration (testcontainers). CD: `deploy.yml` ships apps/api to Fly on green push to main (needs `FLY_API_TOKEN` repo secret set).
- **Known deprecation (follow-up):** routes still use `status.HTTP_422_UNPROCESSABLE_ENTITY` (Starlette renamed it to `_UNPROCESSABLE_CONTENT`); warnings surface in pytest. Low-risk rename per [feedback-latest-lts]; do in a cleanup pass.
- **Demo cost guard:** per-IP daily free-run cap + global 10/s LLM TokenBucket. A determined attacker rotating IPs could still drive hosted-key cost; documented future mitigation = Cloudflare/global daily $ cap (not built — low risk at portfolio scale).

---

## Bootstrap (for a fresh chat picking this up)

Read in this order:

1. **`c:/Users/Shawn/Documents/GitHub/CLAUDE.md`** — project goal + working rules. Auto-loaded.
2. **`c:/Users/Shawn/.claude/projects/c--Users-Shawn-Documents-GitHub/memory/MEMORY.md`** — index of feedback + project memories. Auto-loaded. Pay attention to: `feedback-git-handling` (never run git), `feedback-no-pypi` (no published libs), `feedback-defensibility` (every choice must be defendable), `feedback-ai-use-style` (avoid AI tells), `feedback-phase-wrap` (this doc + DECISIONS update at end of every phase), `feedback-phase-commits` (~10-15 line commit body).
3. **`c:/Users/Shawn/Documents/GitHub/PLAN.md`** — the master plan. Static, doesn't track progress — use this PROGRESS.md for state.
4. **This file (PROGRESS.md)** — current position + last decision + open TODOs.
5. **`promptforge/docs/ARCHITECTURE.md`** — system shape after most recent phase.
6. **`promptforge/docs/DECISIONS.md`** — defendable "Why" for every locked-in decision. Read before any architecture conversation.
7. **Verify locally:** `cd c:/Users/Shawn/Documents/GitHub/promptforge/apps/api && uv run pytest -m "not integration and not e2e" -q` — unit tests should be green without Docker. Integration/e2e need Docker Desktop running.

**Caveman mode** is active by default; respect it. Code/commits/docs ignore caveman mode (write normal English).

**Shawn handles all git.** Never `git init`, `git add`, `git commit`, `git push`, `git status`, `git log`, `git diff`. Just write files; he commits.
