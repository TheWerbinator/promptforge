import { Placeholder } from "@/components/app-shell/placeholder";

export default function EvalsPage() {
  return (
    <Placeholder title="Evals" note="Eval suites, batch runs, and live SSE results land in Phase 7." />
  );
}
